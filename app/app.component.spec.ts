import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <!-- Test build: minimize template to avoid external component dependencies -->
    <div></div>
  `,
  styles: [],
  standalone: false,
})
export class AppComponent {}

