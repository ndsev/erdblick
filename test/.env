#
# Playwright integration test parameters.
#

# Map/layer labels used in the layer tree and backend `/sources` list.
EB_TEST_MAP_NAME=["TestMap"]
EB_TEST_LAYER_NAME=["WayLayer"]

# Camera jump targets as JSON array of arrays: [[lon, lat, level], ...]
EB_TEST_VIEW_POSITION=[[42.5,11.615,13]]
